// Feature flags. Flip and publish — no other wiring needed.
export const FLAGS = {
  // The live ledger (hero card + compact header figures). Ships dark:
  // OFF until the board has real volume worth showing.
  ledger: false,
};
