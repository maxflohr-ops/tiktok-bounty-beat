# SEO workspace — bountysounds.com

Working folder per the `bounty-seo` skill (see `.claude/skills/bounty-seo/`).

- `gsc/` — Google Search Console exports land here, named like
  `queries-last-3-months.csv`, `pages-last-3-months.csv`,
  `queries-last-16-months.csv`, `pages-last-16-months.csv`.
  The keyword-clustering skill works straight from these.

Property: https://bountysounds.com · Sitemap: https://bountysounds.com/sitemap.xml
Goals (v1): rank for clipping-intent terms (see keyword map in the skill),
grow non-branded organic signups from clippers and artists.
